#include<stdio.h>

/**
 * main - where the function runs
 * @argc:  counts the number of arguments
 * @argv:  stores the arguments
 * Return:  returns an integer 0
**/

int main(int argc, char *argv[])
{
(void)argv;

printf("%d \n", (argc - 1));

return (0);
}

